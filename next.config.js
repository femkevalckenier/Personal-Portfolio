/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['femkevalckenier.be', 'localhost', 'i.postimg.cc'],
  },
}

module.exports = nextConfig
